import React from 'react';

const RoomFilters = () => {
    return (
        <div style={{backgroundColor:'red',height:'90vh'}}>
            side bar
        </div>
    );
};

export default RoomFilters;